---
phase: 260422-un8
plan: "01"
subsystem: cms-schema
tags: [keystatic, video, schema, fields]
dependency_graph:
  requires: []
  provides: [works.video, homepage.heroVideo]
  affects: [keystatic.config.ts]
tech_stack:
  added: []
  patterns: [fields.file for arbitrary MIME video uploads]
key_files:
  created:
    - public/videos/works/.gitkeep
    - public/videos/hero/.gitkeep
  modified:
    - keystatic.config.ts
decisions:
  - Used fields.file (not fields.image) for video — accepts arbitrary MIME types including MP4/WebM
  - directory/publicPath pattern mirrors existing images field convention
metrics:
  duration: "< 5 min"
  completed: "2026-04-22"
  tasks_completed: 2
  files_changed: 3
---

# Phase 260422-un8 Plan 01: Video Schema Fields Summary

**One-liner:** Added optional `fields.file` video fields to the works collection and homepage singleton in Keystatic, with upload directories pre-created.

## What Was Done

Both tasks completed in a single atomic pass:

**Task 1 — works.video**
Added an optional `video` field (type `fields.file`) to the `works` collection after the `images` array. Files are stored in `public/videos/works/` and served from `/videos/works/`. Validation `isRequired: false`.

**Task 2 — homepage.heroVideo**
Added an optional `heroVideo` field (type `fields.file`) to the `homepage` singleton after `shopPreviewWorks`. Files stored in `public/videos/hero/`, served from `/videos/hero/`. Validation `isRequired: false`.

Both video upload directories created with `.gitkeep` so Keystatic can write files on first upload.

## Verification

- `npx tsc --noEmit` — passed, no errors
- `public/videos/works/.gitkeep` — exists
- `public/videos/hero/.gitkeep` — exists

## Commits

| Hash | Message |
|------|---------|
| 1caaca4 | feat(260422-un8-01): add video fields to works and homepage schemas |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. These are pure schema additions; no UI component renders these fields yet (intended — UI will be wired in a subsequent plan).

## Threat Flags

None. No new network endpoints or auth paths introduced. Video upload goes through Keystatic's existing admin route which requires GitHub auth in production (T-un8-01-01 accepted per threat model).

## Self-Check: PASSED

- `keystatic.config.ts` modified with both fields — FOUND
- `public/videos/works/.gitkeep` — FOUND
- `public/videos/hero/.gitkeep` — FOUND
- Commit 1caaca4 — FOUND
