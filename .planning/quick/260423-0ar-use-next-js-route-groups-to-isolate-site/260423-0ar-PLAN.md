---
phase: quick-260423-0ar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/(site)/layout.tsx
  - src/app/(site)/page.tsx
  - src/app/(site)/gallery/page.tsx
  - src/app/(site)/gallery/[slug]/page.tsx
  - src/app/(site)/contact/page.tsx
  - src/app/(site)/contact/purchase/page.tsx
  - src/app/(site)/custom-orders/page.tsx
  - src/app/keystatic/layout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Site pages (homepage, gallery, contact, custom-orders) render with SiteHeader and SiteFooter"
    - "Keystatic admin at /keystatic renders without SiteHeader/SiteFooter and without layout offset"
    - "No URLs change — route groups are transparent to routing"
  artifacts:
    - path: "src/app/(site)/layout.tsx"
      provides: "Site-specific layout wrapping SiteHeader + SiteFooter"
      exports: ["default SiteLayout"]
    - path: "src/app/layout.tsx"
      provides: "Minimal root layout with html/body/font setup only"
      exports: ["metadata", "default RootLayout"]
  key_links:
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/SiteHeader"
      via: "import"
    - from: "src/app/(site)/layout.tsx"
      to: "src/components/SiteFooter"
      via: "import"
---

<objective>
Restructure the Next.js app directory using route groups so the root layout becomes a minimal html/body/font shell, and a `(site)` route group layout wraps only the public-facing pages with SiteHeader and SiteFooter. Keystatic admin routes are left unaffected by site layout.

Purpose: Keystatic's SplitView uses `height: 100vh` and injects `body { overflow: hidden }`. The root layout currently renders SiteHeader above all routes, pushing Keystatic's UI down and clipping the bottom — the last form field (video choose-file button) is only visible when overscrolling on macOS.

Output: Clean route group separation — `(site)/` for public pages, `keystatic/` untouched by site layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip root layout to minimal shell and create (site) route group layout</name>
  <files>src/app/layout.tsx, src/app/(site)/layout.tsx</files>
  <action>
1. Update `src/app/layout.tsx` — keep only the minimal shell:
   - Keep: `globals.css` import, `DM_Sans` + `Fraunces` font setup with variables, `metadata` export, `baseMetadata` import, `next/font/google` import, `type Metadata` import
   - Keep the `//TODO SEO optimizations and images` and `//TODO Footer about creator` comments
   - Remove: `SiteHeader` import and usage, `SiteFooter` import and usage, skip-to-content `<a>` tag, `id="main-content"` div, `da` import from messages
   - The body renders only `{children}` directly: `<body className="bg-linen font-sans text-ink antialiased">{children}</body>`

2. Create `src/app/(site)/layout.tsx` with the site chrome:
   - Import `SiteHeader` from `"@/components/SiteHeader"`
   - Import `SiteFooter` from `"@/components/SiteFooter"`
   - Import `da` from `"../../../messages/da.json"` (3 levels up from `src/app/(site)/`)
   - Render: skip-to-content `<a>` (copy exact classes from the current root layout), `<SiteHeader />`, `<div id="main-content" className="min-h-screen">{children}</div>`, `<SiteFooter />`
   - Export a default `SiteLayout` function accepting `{ children: React.ReactNode }`
   - No `<html>` or `<body>` tags — this is a nested layout
  </action>
  <verify>
    <automated>cd /Users/jonasblendstrup/Documents/projects/byblendstrup/code/by-blendstrup && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>root layout has no SiteHeader/SiteFooter/da import; (site)/layout.tsx exists with correct 3-level relative import path for da.json</done>
</task>

<task type="auto">
  <name>Task 2: Move site pages into (site) route group and simplify keystatic layout</name>
  <files>src/app/(site)/page.tsx, src/app/(site)/gallery/page.tsx, src/app/(site)/gallery/[slug]/page.tsx, src/app/(site)/contact/page.tsx, src/app/(site)/contact/purchase/page.tsx, src/app/(site)/custom-orders/page.tsx, src/app/keystatic/layout.tsx</files>
  <action>
1. Move pages into `(site)/` — use `git mv` to preserve history. Run these commands sequentially:
   ```
   git mv src/app/page.tsx src/app/(site)/page.tsx
   git mv src/app/gallery src/app/(site)/gallery
   git mv src/app/contact src/app/(site)/contact
   git mv src/app/custom-orders src/app/(site)/custom-orders
   ```

2. Update `src/app/keystatic/layout.tsx`:
   - Keep: `export const runtime = "nodejs"`
   - Remove: `<html lang="en">` and `<body>` wrapper tags
   - The default export simply returns `<>{children}</>`:
     ```tsx
     export const runtime = "nodejs"
     
     export default function KeystaticLayout({
       children,
     }: {
       children: React.ReactNode
     }) {
       return <>{children}</>
     }
     ```

Do NOT touch: `src/app/admin-login/`, `src/app/api/`, `src/app/keystatic/[[...params]]/`, `src/app/robots.ts`, `src/app/sitemap.ts`, `src/middleware.ts`
  </action>
  <verify>
    <automated>cd /Users/jonasblendstrup/Documents/projects/byblendstrup/code/by-blendstrup && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 6 site pages exist under src/app/(site)/; keystatic/layout.tsx has no html/body tags; TypeScript reports no errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Route group restructure — root layout stripped to html/body/font shell, (site)/ group wraps site pages with header and footer, keystatic layout simplified to fragment passthrough</what-built>
  <how-to-verify>
1. Run `pnpm dev` and open http://localhost:3000 — site header and footer should render on homepage
2. Open http://localhost:3000/gallery — header and footer present
3. Open http://localhost:3000/keystatic — Keystatic admin UI fills the viewport with NO site header/footer above it
4. In Keystatic, open a ceramic item and scroll to the video field — the choose-file button should be fully visible without needing to overscroll
5. Run `pnpm build` — no build errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system / git | Moving files with git mv preserves history; no user-facing trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0ar-01 | Tampering | Route group layout isolation | accept | Route groups are a Next.js convention; no auth boundary change — admin-login middleware remains unchanged |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no errors
- `pnpm build` completes without errors
- All site routes (/, /gallery, /gallery/[slug], /contact, /contact/purchase, /custom-orders) render with SiteHeader + SiteFooter
- /keystatic renders without SiteHeader or SiteFooter
- No URLs changed (route groups are URL-transparent)
</verification>

<success_criteria>
Keystatic admin UI occupies full viewport height with no layout offset from SiteHeader. The video choose-file button is visible without overscrolling. All public site pages continue to render with the site header and footer.
</success_criteria>

<output>
After completion, create `.planning/quick/260423-0ar-use-next-js-route-groups-to-isolate-site/260423-0ar-SUMMARY.md`
</output>
