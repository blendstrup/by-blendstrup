---
phase: 06-polish-launch
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/gallery/page.tsx
  - src/app/gallery/[slug]/page.tsx
  - src/app/contact/page.tsx
  - src/app/contact/purchase/page.tsx
  - src/app/custom-orders/page.tsx
autonomous: true
requirements:
  - DSGN-04
  - I18N-04
must_haves:
  truths:
    - "Root layout exports metadata with title.template = '%s — By Blendstrup'"
    - "Homepage exports metadata with exact title and description from UI-SPEC"
    - "Gallery page exports metadata with exact title and description from UI-SPEC"
    - "Gallery detail page exports async generateMetadata reading work title and first image"
    - "Contact page exports metadata with exact title and description from UI-SPEC"
    - "Purchase inquiry page exports metadata with exact title and description from UI-SPEC"
    - "Custom orders page exports metadata with exact title and description from UI-SPEC"
    - "No page has e-commerce chrome in its metadata (no 'cart', 'checkout', 'rating' in titles/descriptions)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root metadata with title template"
      contains: "template: \"%s — By Blendstrup\""
    - path: "src/app/gallery/[slug]/page.tsx"
      provides: "Dynamic generateMetadata using work title and first image"
      contains: "generateMetadata"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/metadata.ts"
      via: "import { baseMetadata }"
      pattern: "baseMetadata"
    - from: "src/app/gallery/[slug]/page.tsx"
      to: "keystatic.config"
      via: "createReader in generateMetadata"
      pattern: "generateMetadata"
---

<objective>
Add generateMetadata exports to all seven page files. Each page spreads baseMetadata
and overrides title and description with the exact strings from 06-UI-SPEC.md.
The gallery detail page uses an async generateMetadata that reads the work entry
for a dynamic title and OG image.

Purpose: DSGN-04 audit requires no e-commerce chrome anywhere, including metadata.
I18N-04 is retired (Danish-only, no hreflang). This plan closes the SEO metadata
requirement (D-01) across all routes.
Output: Every public route emits correct title, description, and OG metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-launch/06-CONTEXT.md
@.planning/phases/06-polish-launch/06-RESEARCH.md
@.planning/phases/06-polish-launch/06-UI-SPEC.md
@.planning/phases/06-polish-launch/06-02-SUMMARY.md
@.planning/phases/06-polish-launch/06-03-SUMMARY.md

<interfaces>
<!-- baseMetadata — created in Plan 02 at src/lib/metadata.ts -->
<!--
import type { Metadata } from "next"
export const baseMetadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: { default: "By Blendstrup", template: "%s — By Blendstrup" },
  description: "Håndlavede keramikker fra By Blendstrup...",
  openGraph: { siteName: "By Blendstrup", locale: "da_DK", type: "website",
    images: [{ url: "/og-default.jpg", width: 1200, height: 630, alt: "..." }] },
  robots: { index: true, follow: true },
  twitter: { card: "summary_large_image" },
}
-->

<!-- ANTI-PATTERN: shallow openGraph merge (from 06-RESEARCH.md Pitfall 2) -->
<!-- WRONG:  openGraph: { title: "Kontakt" }          ← drops images, siteName, locale -->
<!-- RIGHT:  openGraph: { ...baseMetadata.openGraph, title: "Kontakt — By Blendstrup" } -->

<!-- Exact copy strings from 06-UI-SPEC.md (authoritative — use verbatim): -->
<!--
Root layout:
  title.default: "By Blendstrup"
  title.template: "%s — By Blendstrup"
  description: "Håndlavede keramikker fra By Blendstrup. Unikke stykker til salg og mulighed for at bestille specialfremstillede keramikker."

Homepage (/):
  title: "By Blendstrup — Håndlavet keramik"
  description: "Oplev håndlavede keramikker skabt med omhu. Se aktuelle stykker til salg og bestil din egen specialkeramik."

Gallery (/gallery):
  title: "Keramik"  (template produces "Keramik — By Blendstrup")
  description: "Gennemse alle håndlavede keramikker fra By Blendstrup. Filtrer efter stykker til salg."

Gallery detail (/gallery/[slug]):
  title: work.title  (template produces "[Work title] — By Blendstrup")
  description: "[work.description first 120 chars trimmed at word boundary] — By Blendstrup."

Contact (/contact):
  title: "Kontakt"  (template produces "Kontakt — By Blendstrup")
  description: "Kontakt By Blendstrup for køb af keramik eller generelle spørgsmål."

Purchase inquiry (/contact/purchase):
  title: "Købsforespørgsel"  (produces "Købsforespørgsel — By Blendstrup")
  description: "Send en forespørgsel om køb af et keramikstykke fra By Blendstrup."

Custom orders (/custom-orders):
  title: "Specialbestilling"  (produces "Specialbestilling — By Blendstrup")
  description: "Bestil din egen specialfremstillede keramik fra By Blendstrup. Fortæl hvad du drømmer om."
-->

<!-- NOTE from STATE.md Phase 4 decision: there is NO /shop route. -->
<!-- Shop = gallery?filter=for-sale. Do NOT add shop metadata. -->
<!-- The UI-SPEC has a /shop row but this was before the Phase 4 decision merged shop into gallery. -->
<!-- OMIT the /shop metadata entry. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add metadata to root layout and all static pages (6 files)</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/app/gallery/page.tsx,
    src/app/contact/page.tsx,
    src/app/contact/purchase/page.tsx,
    src/app/custom-orders/page.tsx
  </files>
  <read_first>
    - src/app/layout.tsx (full file — add metadata export alongside existing default export)
    - src/app/page.tsx (full file — add metadata const; note if it has "use client" or other constraints)
    - src/app/gallery/page.tsx (full file — add metadata const)
    - src/app/contact/page.tsx (full file — add metadata const)
    - src/app/contact/purchase/page.tsx (full file — add metadata const)
    - src/app/custom-orders/page.tsx (full file — add metadata const)
    - src/lib/metadata.ts (confirm baseMetadata is exported from Plan 02)
  </read_first>
  <action>
For each file, add a metadata export near the top of the file (after imports, before the
default export). Use `import type { Metadata } from "next"` and
`import { baseMetadata } from "@/lib/metadata"`.

CRITICAL: Always re-spread `...baseMetadata.openGraph` when setting openGraph. Never
set openGraph as a new object without spreading the parent — this would drop the OG
image, siteName, and locale from baseMetadata.

**src/app/layout.tsx — add metadata export:**

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"

export const metadata: Metadata = {
  ...baseMetadata,
  title: {
    default: "By Blendstrup",
    template: "%s — By Blendstrup",
  },
  description:
    "Håndlavede keramikker fra By Blendstrup. Unikke stykker til salg og mulighed for at bestille specialfremstillede keramikker.",
}
```

**src/app/page.tsx — add metadata export:**

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"

export const metadata: Metadata = {
  ...baseMetadata,
  title: "By Blendstrup — Håndlavet keramik",
  description:
    "Oplev håndlavede keramikker skabt med omhu. Se aktuelle stykker til salg og bestil din egen specialkeramik.",
  openGraph: {
    ...baseMetadata.openGraph,
    title: "By Blendstrup — Håndlavet keramik",
    description:
      "Oplev håndlavede keramikker skabt med omhu. Se aktuelle stykker til salg og bestil din egen specialkeramik.",
  },
}
```

**src/app/gallery/page.tsx — add metadata export:**

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"

export const metadata: Metadata = {
  ...baseMetadata,
  title: "Keramik",
  description:
    "Gennemse alle håndlavede keramikker fra By Blendstrup. Filtrer efter stykker til salg.",
  openGraph: {
    ...baseMetadata.openGraph,
    title: "Keramik — By Blendstrup",
    description:
      "Gennemse alle håndlavede keramikker fra By Blendstrup. Filtrer efter stykker til salg.",
  },
}
```

**src/app/contact/page.tsx — add metadata export:**

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"

export const metadata: Metadata = {
  ...baseMetadata,
  title: "Kontakt",
  description:
    "Kontakt By Blendstrup for køb af keramik eller generelle spørgsmål.",
  openGraph: {
    ...baseMetadata.openGraph,
    title: "Kontakt — By Blendstrup",
    description:
      "Kontakt By Blendstrup for køb af keramik eller generelle spørgsmål.",
  },
}
```

**src/app/contact/purchase/page.tsx — add metadata export:**

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"

export const metadata: Metadata = {
  ...baseMetadata,
  title: "Købsforespørgsel",
  description:
    "Send en forespørgsel om køb af et keramikstykke fra By Blendstrup.",
  openGraph: {
    ...baseMetadata.openGraph,
    title: "Købsforespørgsel — By Blendstrup",
    description:
      "Send en forespørgsel om køb af et keramikstykke fra By Blendstrup.",
  },
}
```

**src/app/custom-orders/page.tsx — add metadata export:**

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"

export const metadata: Metadata = {
  ...baseMetadata,
  title: "Specialbestilling",
  description:
    "Bestil din egen specialfremstillede keramik fra By Blendstrup. Fortæl hvad du drømmer om.",
  openGraph: {
    ...baseMetadata.openGraph,
    title: "Specialbestilling — By Blendstrup",
    description:
      "Bestil din egen specialfremstillede keramik fra By Blendstrup. Fortæl hvad du drømmer om.",
  },
}
```

After all six files are updated, run `pnpm build` to confirm TypeScript passes with no errors.
  </action>
  <verify>
    <automated>pnpm build && pnpm biome check src/</automated>
  </verify>
  <done>
    - layout.tsx exports `metadata` with title.template = "%s — By Blendstrup"
    - All 5 other static pages export `metadata` with correct title and description from UI-SPEC
    - All openGraph objects re-spread `...baseMetadata.openGraph`
    - No e-commerce chrome in any metadata string (no cart, checkout, rating, stock)
    - `pnpm build` exits 0
    - `pnpm biome check src/` exits 0
  </done>
  <acceptance_criteria>
    - `grep "template.*By Blendstrup" src/app/layout.tsx` returns a match
    - `grep "baseMetadata" src/app/layout.tsx` returns a match
    - `grep "Keramik" src/app/gallery/page.tsx` returns a match (title string)
    - `grep "Kontakt" src/app/contact/page.tsx` returns a match (title string)
    - `grep "Købsforespørgsel" src/app/contact/purchase/page.tsx` returns a match
    - `grep "Specialbestilling" src/app/custom-orders/page.tsx` returns a match
    - `grep "baseMetadata.openGraph" src/app/page.tsx` returns a match (openGraph spread pattern)
    - `grep "cart\|checkout\|rating\|discount" src/app/layout.tsx src/app/page.tsx src/app/gallery/page.tsx src/app/contact/page.tsx src/app/custom-orders/page.tsx` returns nothing
    - `pnpm build` exits 0
    - `pnpm biome check src/` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add dynamic generateMetadata to gallery detail page</name>
  <files>src/app/gallery/[slug]/page.tsx</files>
  <read_first>
    - src/app/gallery/[slug]/page.tsx (full file — see existing generateStaticParams, createReader import, and work field names)
    - src/lib/metadata.ts (confirm baseMetadata structure for spreading)
    - 06-RESEARCH.md §Pattern 3 (exact generateMetadata function for gallery detail)
  </read_first>
  <action>
Add an async `generateMetadata` function to src/app/gallery/[slug]/page.tsx.
The function reads the work entry from Keystatic to get the title and first image.

Add the following imports at the top (these may already be partially present — check
the existing imports before adding):

```typescript
import type { Metadata } from "next"
import { baseMetadata } from "@/lib/metadata"
```

Add this function BEFORE the `generateStaticParams` and `default` exports:

```typescript
type Props = { params: Promise<{ slug: string }> }

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const { slug } = await params
  const reader = createReader(process.cwd(), keystaticConfig)
  const work = await reader.collections.works.read(slug)

  if (!work || !work.published) {
    return {
      ...baseMetadata,
      title: "Keramik",
    }
  }

  const firstImagePath =
    (work.images as Array<{ image: string | null; alt: string }>)[0]?.image ??
    null
  const firstImageAlt =
    (work.images as Array<{ image: string | null; alt: string }>)[0]?.alt ??
    work.title

  // Trim description at word boundary to ~120 chars
  const rawDesc = work.description ?? ""
  const trimmedDesc =
    rawDesc.length > 120
      ? rawDesc.slice(0, 120).replace(/\s+\S*$/, "") + " — By Blendstrup."
      : rawDesc || undefined

  return {
    ...baseMetadata,
    title: work.title,
    description: trimmedDesc,
    openGraph: {
      ...baseMetadata.openGraph,
      title: `${work.title} — By Blendstrup`,
      description: trimmedDesc,
      ...(firstImagePath
        ? {
            images: [
              {
                url: firstImagePath,
                width: 1200,
                height: 630,
                alt: firstImageAlt,
              },
            ],
          }
        : {}),
    },
  }
}
```

After adding the function, run `pnpm build` to confirm the dynamic route still builds
with `generateStaticParams` and the new `generateMetadata` function co-existing.
  </action>
  <verify>
    <automated>pnpm build && pnpm biome check src/</automated>
  </verify>
  <done>
    - gallery/[slug]/page.tsx exports `generateMetadata` as an async function
    - generateMetadata reads the work entry via createReader (same pattern as the page)
    - Missing/draft work returns fallback metadata (title: "Keramik")
    - Valid work returns title = work.title, openGraph.images uses firstImagePath if available
    - Description trimmed at 120 chars at word boundary
    - `pnpm build` exits 0
    - `pnpm biome check src/` exits 0
  </done>
  <acceptance_criteria>
    - `grep "export async function generateMetadata" src/app/gallery/\[slug\]/page.tsx` returns a match
    - `grep "work.title" src/app/gallery/\[slug\]/page.tsx` returns a match in generateMetadata context
    - `grep "baseMetadata.openGraph" src/app/gallery/\[slug\]/page.tsx` returns a match
    - `grep "120" src/app/gallery/\[slug\]/page.tsx` returns a match (description trim)
    - `pnpm build` exits 0
    - `pnpm biome check src/` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Work title/description → OG metadata | Keystatic-stored content rendered into HTML head metadata |
| Search engine bots → metadata | Bots read metadata from rendered HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | generateMetadata for draft/unpublished works | mitigate | generateMetadata checks `!work.published` and returns generic fallback — no draft content exposed in metadata. Same guard as the page itself (notFound on draft slug) |
| T-06-12 | Tampering | work.description injected into OG description | accept | Keystatic content is owner-authored; not user input; not rendered as HTML (metadata strings are plain text in head tags). No XSS vector in metadata strings. |
| T-06-13 | Information Disclosure | openGraph.images referencing firstImagePath | accept | Images are already public at /gallery/[slug]; OG image merely hints at their location. No private images in /public/images/works/. |
</threat_model>

<verification>
1. `grep "export async function generateMetadata" src/app/gallery/[slug]/page.tsx` — dynamic metadata present
2. `grep "template.*By Blendstrup" src/app/layout.tsx` — title template in root layout
3. `grep "Keramik" src/app/gallery/page.tsx` — gallery title correct
4. `grep "Kontakt" src/app/contact/page.tsx` — contact title correct
5. `grep "cart\|checkout\|discount\|rating" src/` (recursive) — no e-commerce chrome in metadata
6. `pnpm build` exits 0
7. `pnpm test --run` exits 0
8. `pnpm biome check src/` exits 0
</verification>

<success_criteria>
- All 7 page files have metadata exports (per D-01)
- Title template `%s — By Blendstrup` set in root layout (per D-01, em dash from UI-SPEC)
- Gallery detail uses dynamic generateMetadata with work.title and first image (per D-01)
- No e-commerce chrome in any metadata string (DSGN-04 audit passes)
- I18N-04 remains retired — no hreflang exported from any page (D-02)
- Build green, Biome check clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch/06-04-SUMMARY.md`
</output>
