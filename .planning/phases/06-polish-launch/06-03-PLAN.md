---
phase: 06-polish-launch
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/WorkCard.tsx
  - src/components/ShopCard.tsx
  - src/components/WorkDetail.tsx
  - src/app/gallery/page.tsx
  - src/app/gallery/[slug]/page.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - DSGN-02
must_haves:
  truths:
    - "WorkCard uses placeholder='blur' with a blurDataUrl prop from its RSC parent"
    - "ShopCard uses placeholder='blur' with a blurDataUrl prop from its RSC parent"
    - "WorkDetail generates its own blur placeholder using getBlurDataUrl directly (it is already an RSC)"
    - "Gallery index page passes blurDataUrl to each WorkCard"
    - "Gallery detail page renders WorkDetail with real blur placeholders"
    - "Homepage ShopCard instances receive blurDataUrl from the homepage RSC"
    - "No 'use client' directive is added to WorkCard or ShopCard"
  artifacts:
    - path: "src/components/WorkCard.tsx"
      provides: "WorkCard with blur placeholder prop support"
      contains: "blurDataUrl?: string"
    - path: "src/components/ShopCard.tsx"
      provides: "ShopCard with blur placeholder prop support"
      contains: "blurDataUrl?: string"
    - path: "src/components/WorkDetail.tsx"
      provides: "WorkDetail with plaiceholder-generated blur"
      contains: "getBlurDataUrl"
    - path: "src/app/gallery/page.tsx"
      provides: "Gallery RSC computing blurDataUrl for each card"
      contains: "getBlurDataUrl"
    - path: "src/app/gallery/[slug]/page.tsx"
      provides: "Gallery detail page passing blur to WorkDetail"
      contains: "getBlurDataUrl"
  key_links:
    - from: "src/app/gallery/page.tsx"
      to: "src/components/WorkCard.tsx"
      via: "blurDataUrl prop"
      pattern: "blurDataUrl="
    - from: "src/app/page.tsx"
      to: "src/components/ShopCard.tsx"
      via: "blurDataUrl prop"
      pattern: "blurDataUrl="
---

<objective>
Thread blur placeholders through the component tree. WorkCard and ShopCard receive
`blurDataUrl` as a prop from their RSC parents; WorkDetail generates its own.
The gallery index, gallery detail, and homepage RSCs compute blur URLs before
rendering.

Purpose: DSGN-02 requires blur placeholders on all ceramic images. This is the
visual quality deliverable for image loading.
Output: WorkCard, ShopCard, WorkDetail, gallery pages, homepage all use real LQIP blur.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-launch/06-CONTEXT.md
@.planning/phases/06-polish-launch/06-RESEARCH.md
@.planning/phases/06-polish-launch/06-01-SUMMARY.md

<interfaces>
<!-- getBlurDataUrl — created in Plan 01 -->
<!--
import { getBlurDataUrl } from "@/lib/blur-placeholder"
// Signature: (publicPath: string | null) => Promise<string | undefined>
// Called in RSC context (server components, async page functions)
// Returns base64 data URI or undefined (safe to pass to next/image blurDataURL)
-->

<!-- Current WorkCard interface (read src/components/WorkCard.tsx before editing): -->
<!--
interface WorkCardProps {
  slug: string
  entry: WorkCardEntry
  labels: { sold: string; forSale: string }
}
// Add: blurDataUrl?: string
-->

<!-- Current ShopCard interface (read src/components/ShopCard.tsx before editing): -->
<!--
interface ShopCardProps {
  slug: string
  entry: ShopCardEntry
  labels: { sold: string; forSale: string; contactToBuy: string }
}
// Add: blurDataUrl?: string
-->

<!-- next/image blur usage (from 06-RESEARCH.md WorkCard update pattern): -->
<!--
<Image
  src={...}
  alt={...}
  fill
  className="object-cover ..."
  sizes="..."
  placeholder={blurDataUrl ? "blur" : "empty"}
  blurDataURL={blurDataUrl}
/>
-->

<!-- Critical pitfall (from 06-RESEARCH.md Pitfall 4): -->
<!-- WorkCard and ShopCard should NOT call getBlurDataUrl internally. -->
<!-- The blur URL is computed in the RSC parent and passed as a prop. -->
<!-- WorkDetail IS an RSC-compatible component and can call getBlurDataUrl directly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add blurDataUrl prop to WorkCard and ShopCard</name>
  <files>src/components/WorkCard.tsx, src/components/ShopCard.tsx</files>
  <read_first>
    - src/components/WorkCard.tsx (full file — understand current Image usage and props interface)
    - src/components/ShopCard.tsx (full file — understand current Image usage and props interface)
    - 06-RESEARCH.md §Pattern 4 (the exact next/image attribute additions)
  </read_first>
  <action>
**WorkCard.tsx changes:**

1. Add `blurDataUrl?: string` to WorkCardProps interface (after `labels`).
2. Destructure `blurDataUrl` from props.
3. In the `<Image>` component, add two attributes:
   - `placeholder={blurDataUrl ? "blur" : "empty"}`
   - `blurDataURL={blurDataUrl}`

The Image element in WorkCard currently has: `src`, `alt`, `fill`, `className`, `sizes`.
After the change it should have all five plus `placeholder` and `blurDataURL`.

Do NOT remove or change: `fill`, `className`, `sizes` — D-09 says no sizes changes.
Do NOT add `"use client"` to WorkCard.

**ShopCard.tsx changes:**

1. Add `blurDataUrl?: string` to ShopCardProps interface (after `labels`).
2. Destructure `blurDataUrl` from props.
3. In the `<Image>` component, add:
   - `placeholder={blurDataUrl ? "blur" : "empty"}`
   - `blurDataURL={blurDataUrl}`

Same rules: do not change sizes, do not add "use client".

After both edits, run `pnpm build` to confirm TypeScript passes.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    - WorkCardProps contains `blurDataUrl?: string`
    - ShopCardProps contains `blurDataUrl?: string`
    - Both Image elements have `placeholder={blurDataUrl ? "blur" : "empty"}` and `blurDataURL={blurDataUrl}`
    - `pnpm build` exits 0
  </done>
  <acceptance_criteria>
    - `grep "blurDataUrl\?: string" src/components/WorkCard.tsx` returns a match
    - `grep "blurDataUrl\?: string" src/components/ShopCard.tsx` returns a match
    - `grep "blurDataURL={blurDataUrl}" src/components/WorkCard.tsx` returns a match
    - `grep "blurDataURL={blurDataUrl}" src/components/ShopCard.tsx` returns a match
    - `grep "placeholder=" src/components/WorkCard.tsx` returns a match
    - `grep "placeholder=" src/components/ShopCard.tsx` returns a match
    - `grep "use client" src/components/WorkCard.tsx` returns nothing
    - `grep "use client" src/components/ShopCard.tsx` returns nothing
    - `pnpm build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Compute and pass blurDataUrl in gallery pages, homepage, and WorkDetail</name>
  <files>
    src/app/gallery/page.tsx,
    src/app/gallery/[slug]/page.tsx,
    src/app/page.tsx,
    src/components/WorkDetail.tsx
  </files>
  <read_first>
    - src/app/gallery/page.tsx (full file — understand how works array is mapped to WorkCard components)
    - src/app/gallery/[slug]/page.tsx (full file — see how WorkDetail is rendered)
    - src/app/page.tsx (full file — see how ShopCard components are rendered on homepage)
    - src/components/WorkDetail.tsx (full file — understand current images prop and Image usage; find the static BLUR_DATA_URL constant)
    - src/lib/blur-placeholder.ts (confirm getBlurDataUrl signature)
  </read_first>
  <action>
**src/app/gallery/page.tsx:**

1. Import getBlurDataUrl at the top: `import { getBlurDataUrl } from "@/lib/blur-placeholder"`
2. When mapping published works to WorkCard components, compute blur for each work's first image BEFORE the JSX return. Use Promise.all to parallelise:

```typescript
// After filtering published works, before the return statement:
const worksWithBlur = await Promise.all(
  works.map(async (w) => ({
    ...w,
    blurDataUrl: await getBlurDataUrl(w.entry.images[0]?.image ?? null),
  }))
)
```

3. In the JSX, map `worksWithBlur` (not `works`) to WorkCard. Pass `blurDataUrl={w.blurDataUrl}` on each WorkCard.

**src/app/gallery/[slug]/page.tsx:**

1. Import getBlurDataUrl: `import { getBlurDataUrl } from "@/lib/blur-placeholder"`
2. After reading the work entry and before the return, compute blur for the first image:

```typescript
const firstImagePath = (work.images as Array<{ image: string | null; alt: string }>)[0]?.image ?? null
const blurDataUrl = await getBlurDataUrl(firstImagePath)
```

3. WorkDetail currently receives `images` as a prop. WorkDetail handles its own Image rendering internally. Pass the blur for the first image via a new approach:

Since WorkDetail renders ALL images (not just the first), the cleanest approach is to let WorkDetail call getBlurDataUrl internally for each image. Update WorkDetail instead of passing multiple blur URLs from the page:

Update **src/components/WorkDetail.tsx**:
1. Import getBlurDataUrl: `import { getBlurDataUrl } from "@/lib/blur-placeholder"`
2. Make the component async: `export async function WorkDetail({...}): Promise<React.JSX.Element>`
3. Remove the static `BLUR_DATA_URL` constant entirely.
4. For each image rendered via `<Image>`, compute blur inline OR compute all at the top of the function:

```typescript
// At the top of WorkDetail function body, before return:
const blurUrls = await Promise.all(
  images.map((img) => getBlurDataUrl(img.image || null))
)
```

5. In the Image elements, use `blurUrls[index]`:
   - `placeholder={blurUrls[0] ? "blur" : "empty"}`
   - `blurDataURL={blurUrls[0]}`

Apply the same pattern to any secondary images rendered in WorkDetail.

**src/app/page.tsx (homepage):**

1. Import getBlurDataUrl: `import { getBlurDataUrl } from "@/lib/blur-placeholder"`
2. The homepage renders ShopCard components for for-sale pieces. Locate where ShopCards are rendered.
3. Before the return statement, compute blur for each ShopCard's first image:

```typescript
const shopWorksWithBlur = await Promise.all(
  shopPreviewWorks.map(async (w) => ({
    ...w,
    blurDataUrl: await getBlurDataUrl(w.entry.images[0]?.image ?? null),
  }))
)
```

(Use the actual variable name for the array of works rendered as ShopCards — read the homepage file to find it.)

4. Pass `blurDataUrl={w.blurDataUrl}` to each ShopCard JSX element.

After all four files are updated, run `pnpm build` and `pnpm test --run` to confirm.
  </action>
  <verify>
    <automated>pnpm build && pnpm test --run</automated>
  </verify>
  <done>
    - gallery/page.tsx imports getBlurDataUrl and passes blurDataUrl to each WorkCard
    - gallery/[slug]/page.tsx uses WorkDetail which is now async and calls getBlurDataUrl
    - WorkDetail.tsx is async, removes BLUR_DATA_URL constant, calls getBlurDataUrl for each image
    - page.tsx (homepage) imports getBlurDataUrl and passes blurDataUrl to each ShopCard
    - `pnpm build` exits 0
    - `pnpm test --run` exits 0
  </done>
  <acceptance_criteria>
    - `grep "getBlurDataUrl" src/app/gallery/page.tsx` returns a match
    - `grep "blurDataUrl" src/app/gallery/page.tsx` returns a match
    - `grep "getBlurDataUrl" src/components/WorkDetail.tsx` returns a match
    - `grep "BLUR_DATA_URL" src/components/WorkDetail.tsx` returns nothing (constant removed)
    - `grep "async function WorkDetail" src/components/WorkDetail.tsx` returns a match (component is async)
    - `grep "getBlurDataUrl" src/app/page.tsx` returns a match
    - `grep "blurDataUrl" src/app/page.tsx` returns a match
    - `pnpm build` exits 0
    - `pnpm test --run` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keystatic image paths → filesystem | Paths from Keystatic used to resolve blur images at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | blurDataURL base64 in HTML source | accept | Blur placeholder is low-resolution and intentionally embedded — reveals dominant colour of image, not private data |
| T-06-09 | Denial of Service | Promise.all for blur generation on gallery index | accept | gallery/page.tsx is force-dynamic; Promise.all is bounded by number of published works (< 100 for foreseeable future). Each getPlaiceholder call is O(image pixels) on a small file |
| T-06-10 | Tampering | Keystatic image paths used in fs.readFileSync | mitigate | getBlurDataUrl wraps readFileSync in try/catch (implemented in Plan 01); invalid paths return undefined rather than crashing; paths come from developer-committed Keystatic YAML, not user input |
</threat_model>

<verification>
1. `grep "blurDataURL={blurDataUrl}" src/components/WorkCard.tsx` — blur wired in WorkCard
2. `grep "blurDataURL={blurDataUrl}" src/components/ShopCard.tsx` — blur wired in ShopCard
3. `grep "BLUR_DATA_URL" src/components/WorkDetail.tsx` — returns nothing (static constant gone)
4. `grep "getBlurDataUrl" src/app/gallery/page.tsx` — blur computed in gallery RSC
5. `grep "getBlurDataUrl" src/app/page.tsx` — blur computed in homepage RSC
6. `pnpm test --run` exits 0
7. `pnpm build` exits 0
</verification>

<success_criteria>
- All ceramic images in WorkCard, ShopCard, WorkDetail use placeholder="blur" with plaiceholder-generated blurDataURL (per D-07)
- No static shimmer or static BLUR_DATA_URL constants remain in blur-capable components
- No "use client" added to WorkCard or ShopCard
- Build and test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch/06-03-SUMMARY.md`
</output>
