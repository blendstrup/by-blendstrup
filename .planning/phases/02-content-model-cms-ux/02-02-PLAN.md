---
phase: 02-content-model-cms-ux
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified: []
autonomous: false
requirements:
  - CMS-01
  - CMS-02
  - CMS-03
  - CMS-04

must_haves:
  truths:
    - "Owner can open the Keystatic Admin UI and see the Pieces, Taxonomy, and Pages sections in the sidebar"
    - "Owner can create a new ceramic piece with all required fields visible and labelled plainly"
    - "Owner can set draft/published toggle on a piece"
    - "Owner can open the Homepage singleton and pick pieces for the hero and shop preview sections"
    - "Owner can add a new category with Danish and English names"
  artifacts:
    - path: "content/works"
      provides: "At least one test work entry created during smoke test"
    - path: "content/categories"
      provides: "At least one test category entry created during smoke test"
  key_links:
    - from: "Keystatic Admin UI /keystatic/collection/works"
      to: "content/works/*/index.yaml"
      via: "Admin UI save → git commit → YAML file"
      pattern: "content/works"
---

<objective>
Verify the Keystatic schema from Plan 01 works correctly in the Admin UI at `/keystatic`. The owner (or planner proxying for the owner) creates a test ceramic piece, a test category, and curates the homepage singleton — confirming every field label and helper text is clear, all fields are present, and the draft/publish toggle works.

Purpose: Plan 01 delivers the schema in code; this checkpoint confirms it functions correctly in the actual CMS interface that the owner will use. Catches field configuration mistakes (missing labels, broken relationship pickers) that TypeScript cannot catch.

Output: Confirmation that the Admin UI presents all fields as designed. Optional test content entries in `content/works/` and `content/categories/` may be committed or discarded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-model-cms-ux/02-CONTEXT.md
@.planning/phases/02-content-model-cms-ux/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for Admin UI verification</name>
  <files></files>
  <read_first>
    - .planning/phases/02-content-model-cms-ux/02-01-SUMMARY.md — confirm Plan 01 completed successfully before starting the dev server
  </read_first>
  <action>
Start the Next.js development server so the Keystatic Admin UI is accessible for the smoke test checkpoint.

```bash
pnpm dev
```

Confirm the server starts without errors. The Admin UI is at `http://localhost:3000/keystatic`.

If the dev server fails to start (port conflict or error), check the terminal output and resolve before signalling ready for the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/jonasblendstrup/Documents/Projects/byblendstrup/code/by-blendstrup-frontend && pnpm tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit && pnpm vitest run` exits 0 (confirms schema is type-clean and tests green before visual check)
    - Dev server starts with no compilation errors in the terminal output
  </acceptance_criteria>
  <done>Dev server running and build passes. Admin UI accessible at http://localhost:3000/keystatic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Keystatic schema: `works` collection (ceramic pieces with images, bilingual titles and descriptions, three-state sale status, price, lead time, categories, draft/publish toggle), `categories` collection (bilingual), and `homepage` singleton (hero + shop preview relationship pickers). Admin sidebar groups: Pieces | Taxonomy | Pages.
  </what-built>
  <how-to-verify>
    Open http://localhost:3000/keystatic and work through this checklist:

    **1. Sidebar navigation**
    - [ ] Sidebar shows three groups: "Pieces", "Taxonomy", "Pages"
    - [ ] "Pieces" contains "Ceramic Pieces"
    - [ ] "Taxonomy" contains "Categories"
    - [ ] "Pages" contains "Homepage" and "Site Settings"

    **2. Create a test category**
    - Go to Taxonomy → Categories → New entry
    - [ ] Fields shown: "Category name (for URL)", "Name in Danish", "Name in English"
    - [ ] Each field has a helper text description visible below the label
    - Fill in: slug auto-generates, nameDa = "Skåle", nameEn = "Bowls"
    - Save
    - [ ] Category appears in the Categories list

    **3. Create a test ceramic piece**
    - Go to Pieces → Ceramic Pieces → New entry
    - [ ] Fields shown in order: Piece name (slug), Published, Title (Danish), Title (English), Description (Danish), Description (English), Sale status, Price, Lead time / availability note, Categories, Images
    - [ ] "Published" is a checkbox, unchecked by default
    - [ ] "Sale status" is a dropdown showing "Available for purchase", "Sold", "Not for sale" — defaulting to "Not for sale"
    - [ ] "Categories" is a multi-select/combobox showing the "Bowls" category created above
    - [ ] "Images" section allows uploading a photo with an alt text field per image
    - [ ] Every field has a description/helper text visible below the label
    - Fill in: titleDa = "Testskål", titleEn = "Test Bowl", saleStatus = "Available for purchase", price = "800 kr.", one image with alt text
    - Save (as draft — keep Published unchecked)
    - [ ] Piece appears in the Ceramic Pieces list with "saleStatus" and "published" columns visible

    **4. Verify the list view columns**
    - [ ] The Ceramic Pieces list view shows a "saleStatus" column and a "published" column at a glance (not just the title)

    **5. Homepage singleton**
    - Go to Pages → Homepage
    - [ ] "Hero pieces" field shows a relationship picker — can select the test piece created above
    - [ ] "Shop preview pieces" field shows a relationship picker — can select pieces
    - [ ] Both fields have helper text explaining their purpose
    - Pick the test piece as a hero piece and save

    **6. Draft/publish verification**
    - [ ] The test piece saved as draft (Published unchecked) — note that draft visibility check on the public site is a Phase 3 concern; confirming the field exists and saves correctly is sufficient here
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checklist items pass.
    If items fail, describe which field or section is missing or broken so the planner can issue a gap closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localhost Admin UI | Dev-only; no production data. Test content created here is committed to git but is not a security concern. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Test content entries created during smoke test | accept | Test entries (e.g., "Test Bowl") are harmless lorem-ipsum stand-ins. Owner or planner should delete test entries before launch, but they pose no security risk. |
| T-02-06 | Tampering | Admin UI accessible without auth in local dev | accept | Local dev only — no external access. Production Keystatic requires GitHub auth via Keystatic Cloud. Acceptable for smoke testing. |
</threat_model>

<verification>
After checkpoint approval:

```bash
pnpm build   # confirm build still passes after any test content was created
pnpm check   # confirm Biome lint/format clean
```

Confirm content directories exist (if test entries were saved):
```bash
ls content/works/ 2>/dev/null && echo "works content exists" || echo "no test works"
ls content/categories/ 2>/dev/null && echo "categories content exists" || echo "no test categories"
```
</verification>

<success_criteria>
- Owner (or planner) successfully navigated to all three schema areas in the Admin UI
- Every field in the works form was visible with plain-language labels and helper text
- The categories relationship picker worked (selected a category in the works form)
- The homepage singleton relationship pickers showed available works
- Draft/publish toggle was functional
- No JavaScript errors or broken UI observed during the smoke test
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-model-cms-ux/02-02-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
