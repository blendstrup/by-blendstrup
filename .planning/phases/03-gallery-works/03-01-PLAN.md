---
phase: 03-gallery-works
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - keystatic.config.ts
  - messages/en.json
  - messages/da.json
  - src/__tests__/gallery-filter.test.ts
  - src/__tests__/i18n-fields.test.ts
autonomous: true
requirements:
  - GALL-01
  - GALL-02
  - GALL-03
  - GALL-04

must_haves:
  truths:
    - "Keystatic schema exports works collection, categories collection, and homepage singleton"
    - "Existing schema regression test (keystatic-schema.test.ts) passes green after schema restore"
    - "Both message files contain gallery.* namespace with all required keys in EN and DA"
    - "gallery-filter.test.ts covers published-filter logic, for-sale filter, and sold badge status"
    - "i18n-fields.test.ts asserts all gallery.* keys are present in both locales"
    - "pnpm test exits 0 with all test suites green"
  artifacts:
    - path: keystatic.config.ts
      provides: "works + categories + homepage schema with all bilingual fields"
      contains: "collections.works"
    - path: messages/en.json
      provides: "English gallery namespace"
      contains: "gallery"
    - path: messages/da.json
      provides: "Danish gallery namespace"
      contains: "gallery"
    - path: src/__tests__/gallery-filter.test.ts
      provides: "Unit tests for filter logic covering GALL-01, GALL-03, GALL-04, CMS-02 draft gate"
      exports: []
    - path: src/__tests__/i18n-fields.test.ts
      provides: "Extended i18n regression test including gallery.* keys"
      exports: []
  key_links:
    - from: keystatic.config.ts
      to: content/works/
      via: "path: 'content/works/*/'"
      pattern: "content/works/\\*/"
    - from: keystatic.config.ts
      to: keystatic.config.ts collections.categories
      via: "fields.multiRelationship({ collection: 'categories' })"
      pattern: "multiRelationship"
    - from: messages/en.json gallery.*
      to: messages/da.json gallery.*
      via: "identical key structure"
      pattern: "gallery"
---

<objective>
Restore the Keystatic schema (works + categories + homepage) that was removed post-Phase-2, add the gallery i18n namespace to both message files, and create Wave 0 unit tests so the test suite is green before any UI code is written.

Purpose: The Reader API calls in Plans 02 and 03 require `cfg.collections.works` and `cfg.collections.categories` to exist in keystatic.config.ts. The existing keystatic-schema.test.ts is currently FAILING because the schema is absent — this plan makes it green. The gallery-filter.test.ts does not yet exist — this plan creates it so plans 02 and 03 can verify against a real test suite.

Output: A fully-restored keystatic.config.ts (re-verified by the existing schema test), both message files extended with the gallery namespace, and two test files covering all gallery filter logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-gallery-works/03-CONTEXT.md
@.planning/phases/03-gallery-works/03-RESEARCH.md
@.planning/phases/03-gallery-works/03-VALIDATION.md

<interfaces>
<!-- Keystatic field API — confirmed from @keystatic/core 0.5.50 installed -->
<!-- Use these exact imports and field constructors. -->

From keystatic.config.ts (current state — only settings singleton):
```typescript
import { config, singleton, fields } from "@keystatic/core";
// Need to also import: collection
```

Phase 2 VERIFICATION.md confirmed schema shape (verbatim field names, restore exactly):

works collection:
- slug: fields.slug({ name: { label: "URL Slug" } })
- published: fields.checkbox({ label: "Published", description: "...", defaultValue: false })
- titleDa: fields.text({ label: "Title (Danish)", ... })
- titleEn: fields.text({ label: "Title (English)", ... })
- descriptionDa: fields.text({ label: "Description (Danish)", multiline: true, ... })
- descriptionEn: fields.text({ label: "Description (English)", multiline: true, ... })
- saleStatus: fields.select({ label: "Sale Status", options: [{ label: "For Sale", value: "available" }, { label: "Sold", value: "sold" }, { label: "Portfolio Only", value: "notListed" }], defaultValue: "notListed" })
- price: fields.text({ label: "Price", ... })
- leadTime: fields.text({ label: "Lead Time", ... })
- categories: fields.multiRelationship({ label: "Categories", collection: "categories" })
- images: fields.array(fields.object({ image: fields.image({ ... }), alt: fields.text({ ... }) }), { label: "Images" })

categories collection:
- name: fields.slug({ name: { label: "Internal Name" } })
- nameDa: fields.text({ label: "Name (Danish)" })
- nameEn: fields.text({ label: "Name (English)" })

homepage singleton:
- heroWorks: fields.multiRelationship({ label: "Hero Pieces", collection: "works", validation: { length: { max: 3 } } })
- shopPreviewWorks: fields.multiRelationship({ label: "Shop Preview Pieces", collection: "works", validation: { length: { max: 6 } } })

ui.navigation groups: "Pieces" → [collections.works], "Taxonomy" → [collections.categories], "Pages" → [singletons.homepage, singletons.settings]

Existing test assertions in src/__tests__/keystatic-schema.test.ts:
- cfg.collections must have property "works"
- cfg.collections must have property "categories"
- cfg.singletons must have property "settings"
- cfg.singletons must have property "homepage"
- cfg.ui?.navigation must be defined
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore Keystatic schema (works + categories + homepage)</name>
  <files>keystatic.config.ts</files>
  <read_first>
    - keystatic.config.ts (current state — only settings singleton, must read to see existing structure before editing)
    - .planning/phases/02-content-model-cms-ux/02-VERIFICATION.md (canonical schema field names — read "Required Artifacts" and "Key Link Verification" sections; these are the verbatim field definitions that the regression test was written against)
    - src/__tests__/keystatic-schema.test.ts (assertions this task must satisfy)
    - content/works/bowl-test/index.yaml (confirms saleStatus value strings: "sold", "available", "notListed")
  </read_first>
  <behavior>
    - reader.collections.works is defined after importing config
    - reader.collections.categories is defined after importing config
    - reader.singletons.homepage is defined after importing config
    - cfg.ui.navigation is defined after importing config
    - Existing keystatic-schema.test.ts 5/5 tests pass (was failing before this task)
    - pnpm tsc --noEmit exits 0 (no TypeScript errors in schema)
  </behavior>
  <action>
    Edit keystatic.config.ts to add the `collection` import alongside existing `config, singleton, fields`.

    Add these three named consts ABOVE the `export default config(...)` call:

    1. `categories` collection — path: `content/categories/*`, format: yaml, schema: { name: fields.slug, nameDa: fields.text, nameEn: fields.text }. Label: "Categories". All fields must have label and description in plain English.

    2. `works` collection — path: `content/works/*/`, format: yaml, schema with these exact field names (verbatim from Phase 2 VERIFICATION.md):
       - `published`: fields.checkbox({ label: "Published", description: "When checked, this piece appears on the public website.", defaultValue: false })
       - `titleDa`: fields.text({ label: "Title (Danish)", description: "The piece title shown to Danish visitors.", validation: { length: { min: 1, max: 80 } } })
       - `titleEn`: fields.text({ label: "Title (English)", description: "The piece title shown to English visitors.", validation: { length: { min: 1, max: 80 } } })
       - `descriptionDa`: fields.text({ label: "Description (Danish)", description: "Description shown to Danish visitors.", multiline: true })
       - `descriptionEn`: fields.text({ label: "Description (English)", description: "Description shown to English visitors.", multiline: true })
       - `saleStatus`: fields.select({ label: "Sale Status", description: "Is this piece available, sold, or only in the portfolio?", options: [{ label: "For Sale", value: "available" }, { label: "Sold", value: "sold" }, { label: "Portfolio Only", value: "notListed" }], defaultValue: "notListed" })
       - `price`: fields.text({ label: "Price", description: "e.g. 1.200 kr. Leave blank if not for sale.", validation: { length: { max: 40 } } })
       - `leadTime`: fields.text({ label: "Lead Time", description: "e.g. 'Ships in 1–2 weeks'. Leave blank if not for sale.", validation: { length: { max: 80 } } })
       - `categories`: fields.multiRelationship({ label: "Categories", description: "Which category does this piece belong to?", collection: "categories" })
       - `images`: fields.array(fields.object({ image: fields.image({ label: "Image", directory: "public/images/works", publicPath: "/images/works/" }), alt: fields.text({ label: "Alt text", description: "Describe the image for screen readers and search engines.", validation: { length: { min: 1 } } }) }), { label: "Images", description: "Add one or more photos of this piece.", itemLabel: (props) => props.fields.alt.value || "Image" })

    3. `homepage` singleton — path: `content/homepage`, format: yaml, schema:
       - `heroWorks`: fields.multiRelationship({ label: "Hero Pieces", description: "Up to 3 pieces shown in the homepage hero.", collection: "works", validation: { length: { max: 3 } } })
       - `shopPreviewWorks`: fields.multiRelationship({ label: "Shop Preview Pieces", description: "Up to 6 for-sale pieces shown in the homepage shop preview.", collection: "works", validation: { length: { max: 6 } } })

    Update the `export default config(...)` call to include:
    - `collections: { works, categories }` (wire both collections)
    - `singletons: { settings, homepage }` (add homepage alongside existing settings)
    - `ui: { brand: { name: "By Blendstrup" }, navigation: { Pieces: ["collections.works"], Taxonomy: ["collections.categories"], Pages: ["singletons.homepage", "singletons.settings"] } }`

    After editing, run: `pnpm test src/__tests__/keystatic-schema.test.ts` — all 5 tests must pass.
    Then run: `pnpm tsc --noEmit` — must exit 0.
  </action>
  <verify>
    <automated>cd /Users/jonasblendstrup/Documents/Projects/byblendstrup/code/by-blendstrup-frontend && pnpm test src/__tests__/keystatic-schema.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - keystatic.config.ts imports `collection` from "@keystatic/core"
    - keystatic.config.ts contains `works` collection const with `published`, `titleDa`, `titleEn`, `descriptionDa`, `descriptionEn`, `saleStatus`, `price`, `leadTime`, `categories`, `images` fields
    - keystatic.config.ts contains `categories` collection const with `nameDa`, `nameEn` fields
    - keystatic.config.ts contains `homepage` singleton const with `heroWorks`, `shopPreviewWorks` fields
    - keystatic.config.ts `export default config(...)` includes `collections: { works, categories }` and `singletons: { settings, homepage }`
    - keystatic.config.ts `ui.navigation` includes all three groups (Pieces, Taxonomy, Pages)
    - `pnpm test src/__tests__/keystatic-schema.test.ts` → 5 passed, 0 failed
    - `pnpm tsc --noEmit` → exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gallery i18n keys + Wave 0 test files</name>
  <files>
    messages/en.json
    messages/da.json
    src/__tests__/gallery-filter.test.ts
    src/__tests__/i18n-fields.test.ts
  </files>
  <read_first>
    - messages/en.json (current state — must read before editing to preserve existing keys)
    - messages/da.json (current state — must read before editing to preserve existing keys)
    - src/__tests__/i18n-fields.test.ts (current state — must read before extending)
    - .planning/phases/03-gallery-works/03-UI-SPEC.md (copywriting contract section — authoritative EN and DA strings)
    - .planning/phases/03-gallery-works/03-RESEARCH.md (i18n keys list and gallery-filter test descriptions)
    - .planning/phases/03-gallery-works/03-VALIDATION.md (Wave 0 test coverage requirements)
  </read_first>
  <behavior>
    - en.json contains gallery.title = "Works"
    - en.json contains gallery.filterAll = "All"
    - en.json contains gallery.filterForSale = "For Sale"
    - en.json contains gallery.soldLabel = "Sold"
    - en.json contains gallery.forSaleLabel = "For Sale"
    - en.json contains gallery.contactToBuy = "Contact to buy"
    - en.json contains gallery.soldMessage = "This piece has been sold."
    - en.json contains gallery.soldCta = "Commission something similar"
    - en.json contains gallery.emptyAllHeading = "No works yet."
    - en.json contains gallery.emptyAllBody with content
    - en.json contains gallery.emptyForSaleHeading = "Nothing for sale right now."
    - en.json contains gallery.emptyForSaleBody with content
    - en.json contains gallery.emptyForSaleCta = "Commission a custom piece"
    - da.json contains identical key structure with Danish equivalents
    - gallery-filter.test.ts → tests published filter + for-sale filter + sold status logic all pass
    - i18n-fields.test.ts → all gallery.* key assertions pass for both locales
    - pnpm test exits 0
  </behavior>
  <action>
    STEP A — Message files:

    Add a `gallery` key to BOTH `messages/en.json` and `messages/da.json`, preserving all existing keys.

    English additions (from 03-UI-SPEC.md copywriting contract):
    ```json
    "gallery": {
      "title": "Works",
      "filterAll": "All",
      "filterForSale": "For Sale",
      "soldLabel": "Sold",
      "forSaleLabel": "For Sale",
      "contactToBuy": "Contact to buy",
      "soldMessage": "This piece has been sold.",
      "soldCta": "Commission something similar",
      "emptyAllHeading": "No works yet.",
      "emptyAllBody": "Check back soon — new pieces are added regularly.",
      "emptyForSaleHeading": "Nothing for sale right now.",
      "emptyForSaleBody": "All pieces are currently sold or not listed. Interested in something custom?",
      "emptyForSaleCta": "Commission a custom piece"
    }
    ```

    Danish additions (from 03-UI-SPEC.md copywriting contract):
    ```json
    "gallery": {
      "title": "Arbejder",
      "filterAll": "Alle",
      "filterForSale": "Til salg",
      "soldLabel": "Solgt",
      "forSaleLabel": "Til salg",
      "contactToBuy": "Kontakt for køb",
      "soldMessage": "Dette stykke er solgt.",
      "soldCta": "Bestil noget tilsvarende",
      "emptyAllHeading": "Ingen arbejder endnu.",
      "emptyAllBody": "Kom tilbage snart — nye stykker tilføjes løbende.",
      "emptyForSaleHeading": "Intet til salg lige nu.",
      "emptyForSaleBody": "Alle stykker er solgt eller ikke annonceret. Interesseret i noget skræddersyet?",
      "emptyForSaleCta": "Bestil et skræddersyet stykke"
    }
    ```

    STEP B — gallery-filter.test.ts:

    Create `src/__tests__/gallery-filter.test.ts`. This file tests PURE filter logic using plain objects (no Keystatic Reader mocking needed — just arrays of work-shaped objects).

    The file must test these behaviors:
    1. `publishedFilter` — filters out entries where `published === false`; keeps `published === true`
    2. `forSaleFilter` — from a published-only array, filters to `saleStatus === "available"` only
    3. Sold status — a work with `saleStatus === "sold"` passes the published filter but is excluded by for-sale filter
    4. `notListed` status — a portfolio-only work passes the published filter but is excluded by for-sale filter
    5. Empty array input — both filters return empty array without throwing

    Example structure (implement in the test file itself — no external source file needed):
    ```typescript
    // Pure functions extracted inline for testing — these mirror what the RSC will do
    const publishedFilter = (works: Array<{ entry: { published: boolean } }>) =>
      works.filter(w => w.entry.published)

    const forSaleFilter = (works: Array<{ entry: { saleStatus: string } }>) =>
      works.filter(w => w.entry.saleStatus === "available")
    ```

    STEP C — Extend i18n-fields.test.ts:

    Read the current file first. Add a new `describe` block for the gallery namespace that asserts every gallery.* key is defined in both `en.json` and `da.json`. Use the same pattern as the existing shop.saleStatus assertions. Assert at minimum: `gallery.title`, `gallery.filterAll`, `gallery.filterForSale`, `gallery.soldLabel`, `gallery.forSaleLabel`, `gallery.contactToBuy`, `gallery.soldMessage`, `gallery.soldCta`, `gallery.emptyForSaleCta`.

    After all file edits run: `pnpm test` — all suites must be green (currently 11 tests; this task adds new ones).
  </action>
  <verify>
    <automated>cd /Users/jonasblendstrup/Documents/Projects/byblendstrup/code/by-blendstrup-frontend && pnpm test --reporter=verbose</automated>
  </verify>
  <done>
    - messages/en.json contains `"gallery"` key with all 13 sub-keys listed above
    - messages/da.json contains `"gallery"` key with identical structure and Danish values
    - src/__tests__/gallery-filter.test.ts exists and tests 5 behaviors (published filter, for-sale filter, sold exclusion, notListed exclusion, empty input)
    - src/__tests__/i18n-fields.test.ts extended with gallery.* assertions for both locales
    - `pnpm test` exits 0 — all suites green (includes schema test, i18n test, gallery-filter test)
    - `pnpm biome check src/` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keystatic.config.ts → Reader API | Schema shape must be correct; wrong field names produce TypeScript errors caught at build time, not runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | keystatic.config.ts field `published` | mitigate | Phase 3 RSCs MUST filter `w.entry.published === true` before rendering; this plan's test (gallery-filter.test.ts) verifies the filter logic; enforcement verified by test |
| T-3-02 | Tampering | content/works/\* YAML files | accept | Git-based content; only repo contributors can write YAML; no external input path |
| T-3-03 | Information Disclosure | i18n keys in messages/*.json | accept | No secrets in message files; all strings are public UI copy |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/jonasblendstrup/Documents/Projects/byblendstrup/code/by-blendstrup-frontend

# Full test suite (must be all green)
pnpm test

# TypeScript must compile
pnpm tsc --noEmit

# Biome lint must be clean
pnpm biome check src/

# Schema file sanity checks
grep -c "collections.works\|collections\.works" keystatic.config.ts
grep "gallery" messages/en.json
grep "gallery" messages/da.json
```

All checks must pass before Plan 02 executes.
</verification>

<success_criteria>
- `pnpm test` → all suites pass (schema test 5/5, i18n test includes gallery.* assertions, gallery-filter test 5+ tests green)
- `pnpm tsc --noEmit` → exits 0
- `pnpm biome check src/` → exits 0
- keystatic.config.ts exports `collections.works`, `collections.categories`, `singletons.homepage`, `ui.navigation`
- Both message files contain the `gallery` namespace with identical key structure (13 keys)
- gallery-filter.test.ts exists and tests published + for-sale filter logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-gallery-works/03-01-SUMMARY.md`
</output>
