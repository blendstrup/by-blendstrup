---
phase: 1
slug: foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-18
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for the Foundation phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> This phase establishes the design system that all future phases inherit. It is authoritative for tokens, typography, and color across the entire project.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn/ui explicitly rejected — see CLAUDE.md) |
| Preset | not applicable |
| Component library | Radix UI primitives only, pulled individually as needed |
| Icon library | lucide-react |
| Font | Fraunces (serif display) + DM Sans (humanist sans body) via next/font |

**Font rationale:** Fraunces is a variable optical-size serif with warmth that suits handmade craft; DM Sans is clean and neutral at body sizes. Both are Google Fonts, self-hosted via `next/font` for zero CLS. Alternative: Cormorant Garamond (serif) + Inter (sans) — acceptable substitution with identical implementation pattern.

---

## Spacing Scale

Declared values (multiples of 4 only). Tailwind v4 `@theme` maps these as CSS custom properties:

| Token | Value | Tailwind class | Usage |
|-------|-------|----------------|-------|
| xs | 4px | `gap-1` / `p-1` | Icon gaps, inline label padding |
| sm | 8px | `gap-2` / `p-2` | Tight element separation |
| md | 16px | `gap-4` / `p-4` | Default component spacing |
| lg | 24px | `gap-6` / `p-6` | Card internal padding |
| xl | 32px | `gap-8` / `p-8` | Section sub-element gaps |
| 2xl | 48px | `gap-12` / `p-12` | Section padding (horizontal page margins, mobile) |
| 3xl | 64px | `gap-16` / `p-16` | Section padding (horizontal page margins, desktop) |
| 4xl | 96px | `gap-24` / `p-24` | Major vertical section breaks |

**Japandi rule:** Default to the larger end of the scale. Prefer `p-12`/`p-16` over `p-6` for section padding. Prefer `gap-8`/`gap-12` over `gap-4` for grid gaps. Whitespace is the primary design element.

Exceptions:
- Language toggle touch target: minimum 44px height (accessibility — meets WCAG 2.5.5 AA)
- Header height: 64px (aligns to 3xl token)

---

## Typography

Two typefaces, four sizes, two weights. No exceptions in Phase 1.

| Role | Typeface | Size | Weight | Line Height | Tailwind class |
|------|----------|------|--------|-------------|----------------|
| Body | DM Sans | 16px | 400 (regular) | 1.6 | `text-base font-normal` |
| Label / UI | DM Sans | 14px | 500 (medium) | 1.4 | `text-sm font-medium` |
| Heading | Fraunces | 28px | 400 (regular) | 1.2 | `text-[28px] font-normal` |
| Display | Fraunces | 48px | 400 (regular) | 1.1 | `text-5xl font-normal` |

**Weights in use:** 400 (regular) and 500 (medium) only. No bold (700) in Phase 1 — Japandi restraint avoids heavy weight contrast. Fraunces optical-size variation creates hierarchy through size alone, not weight.

**Letter-spacing:** Display and Heading use `tracking-tight` (-0.01em). Body and Label use default tracking.

**Serif display usage:** Fraunces is reserved for headings and display text only. All UI chrome (nav labels, buttons, form labels, captions) uses DM Sans.

---

## Color

Tailwind v4 `@theme` block declares these as CSS custom properties. The default Tailwind palette is entirely overridden — no blue-500, red-400, etc. accessible to developers.

| Role | Token name | Hex | Usage |
|------|-----------|-----|-------|
| Dominant (60%) | `--color-linen` | `#F5F0E8` | Page background, primary surface |
| Secondary (30%) | `--color-oat` | `#EDE7D9` | Header/nav background, card surfaces, sidebar |
| Neutral mid | `--color-clay` | `#C4A882` | Borders, dividers, subtle decoration |
| Neutral dark | `--color-stone` | `#7A6A58` | Secondary text, captions, placeholder text |
| Foreground | `--color-ink` | `#2C2418` | Primary body text, headings |
| Accent (10%) | `--color-terracotta` | `#A85C3A` | Reserved elements only (see below) |
| Destructive | `--color-fault` | `#8B3A2A` | Destructive actions only |
| Surface inverse | `--color-ink-surface` | `#1E1A12` | Footer background (dark anchor) |

**60/30/10 split in practice:**
- 60% linen: `<body>` background, all page-level backgrounds
- 30% oat: `<header>`, card backgrounds, any panel or sidebar
- 10% terracotta: accent-reserved elements only

**Accent (terracotta) reserved for:**
1. Language toggle active indicator (the currently selected locale)
2. Focus ring outline on interactive elements (keyboard navigation)
3. No other uses in Phase 1 — future phases may extend this list

**Destructive (fault):** Not used in Phase 1 — no destructive actions exist in the foundation scaffold.

---

## Copywriting Contract

Phase 1 delivers a locale-aware placeholder shell. Copy is minimal and functional.

| Element | Danish (DA) | English (EN) |
|---------|-------------|--------------|
| Site name (nav) | By Blendstrup | By Blendstrup |
| Placeholder page heading | Kommer snart | Coming soon |
| Placeholder page body | Siden er under opbygning. | The site is being built. |
| Language toggle — switch to Danish | Dansk | Dansk |
| Language toggle — switch to English | English | English |
| Skip to content (a11y) | Spring til indhold | Skip to content |

**Notes on copy:**
- "By Blendstrup" is the brand name — never translated.
- Language toggle labels always show the language's own name (not translated): "Dansk" in both locales, "English" in both locales.
- Empty state: not applicable in Phase 1 — no data-driven content exists yet.
- Error state: not applicable in Phase 1 — no forms or data fetching.
- Destructive confirmation: not applicable in Phase 1 — no destructive actions.

---

## Interaction Contracts

### Language Toggle (I18N-01)

- Rendered in the site header as a text control, not a dropdown.
- Pattern: two locale codes side by side, separated by a hairline divider: `DA | EN`
- Active locale: terracotta (`--color-terracotta`) text at 500 weight.
- Inactive locale: stone (`--color-stone`) text at 400 weight, with underline on hover.
- Touch target: minimum 44px height.
- On activation: `router.push()` to the matching locale path — full page navigation, no client-side partial update.
- No animation in Phase 1 — that is addressed in Phase 6.

### Focus Management

- All interactive elements show a 2px solid terracotta focus ring with 2px offset (`outline: 2px solid var(--color-terracotta); outline-offset: 2px`).
- No `:focus` styles suppressed — only `:focus-visible` used so mouse users do not see rings.

### Skip Link (a11y)

- First focusable element in the DOM.
- Visually hidden until focused, then appears at top-left of viewport.
- Links to `#main-content`.

---

## Layout Shell

Phase 1 delivers the layout shell that all pages inherit.

| Element | Specification |
|---------|---------------|
| Max content width | 1280px, centered (`max-w-screen-xl mx-auto`) |
| Header height | 64px, sticky |
| Header padding | `px-12` (mobile), `px-16` (desktop ≥1024px) |
| Page horizontal padding | `px-6` (mobile), `px-12` (tablet ≥768px), `px-16` (desktop ≥1024px) |
| Footer padding | `py-12 px-12` (mobile), `py-16 px-16` (desktop) |
| Header background | oat (`--color-oat`) |
| Footer background | ink-surface (`--color-ink-surface`) |
| Footer text | linen (`--color-linen`) at 80% opacity |

**Responsive breakpoints** (Tailwind v4 defaults, not overridden):
- Mobile: `<640px`
- Tablet: `640px–1023px`
- Desktop: `≥1024px`

---

## Registry Safety

No shadcn/ui. No third-party component registries. All components in Phase 1 are built from scratch using Tailwind v4 utilities and (optionally) Radix UI primitives pulled directly from npm.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Tailwind v4 @theme Block (Reference)

The executor should declare the following in the root CSS file. This is the authoritative token source for all phases:

```css
@import "tailwindcss";

@theme {
  /* Typefaces */
  --font-sans: "DM Sans", ui-sans-serif, system-ui, sans-serif;
  --font-serif: "Fraunces", ui-serif, Georgia, serif;

  /* Color palette — full override, default palette removed */
  --color-linen: #F5F0E8;
  --color-oat: #EDE7D9;
  --color-clay: #C4A882;
  --color-stone: #7A6A58;
  --color-ink: #2C2418;
  --color-terracotta: #A85C3A;
  --color-fault: #8B3A2A;
  --color-ink-surface: #1E1A12;

  /* Spacing — augment Tailwind defaults with named tokens */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --spacing-3xl: 64px;
  --spacing-4xl: 96px;
}
```

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
